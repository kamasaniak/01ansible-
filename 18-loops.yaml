- name: install packages
  hosts: local 
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.debug:
    name: "{{ item }}"
    state: installed
  loop:
  - gcc 
  - mysql
  - git 
  - zip 
  